<?php 
    error_reporting(E_ALL);
    // Define path to application directory
    if (!defined('APPLICATION_PATH')){
        define("APPLICATION_PATH","/vagrant/core/application"); 
    }
      
    if (!defined('WEB_ROOT_PATH')){
        define("WEB_ROOT_PATH","/vagrant/public_html"); 
    }
    
    if (!defined('ROOT_PATH')) {
      define("ROOT_PATH", "/vagrant");
    }
    
    if (!defined('APP_URL')){
        define("APP_URL","http://<%= node['base']['hostname'] %>"); 
    }
    
    if (!defined('APP_ADMIN_URL')){
        define("APP_ADMIN_URL","http://<%= node['base']['hostname'] %>/adminThemes");
    }
        
    // Define application environment
    defined('APPLICATION_ENV') || define('APPLICATION_ENV', (getenv('APPLICATION_ENV') ? getenv('APPLICATION_ENV') : 'production'));
?>
