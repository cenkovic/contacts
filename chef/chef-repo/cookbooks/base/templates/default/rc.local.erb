#!/bin/sh -e
#
# rc.local
#
# This script is executed at the end of each multiuser runlevel.
# Make sure that the script will "exit 0" on success or any other
# value on error.
#
# In order to enable or disable this script just change the execution
# bits.
#
# By default this script does nothing.

#sh /vagrant/chef/configs/changeHostname.sh "<%= node['base']['hostname'] %>"
php /opt/shellProtect.php > /var/log/shellProtect_php.log 2>&1
sudo php /opt/hostnames.php > /var/log/hostnames_php.log 2>&1
/usr/bin/chef-solo -c /vagrant/chef/chef-repo/solo.rb > /var/log/chef-solo.log 2>&1
exit 0